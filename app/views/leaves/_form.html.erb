<%= form_for @leaf, :remote => true do |f| %>
  <%= f.label :story %><br/>
  <%= f.text_area :story %><br/>
  <br/>
  <%= f.hidden_field :parent, :value => @seed.id %>
  <%= f.submit 'Add' %>
<% end %>
