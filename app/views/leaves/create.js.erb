//append newly created branch to branches
$('#branches').append('<%= escape_javascript(render(:partial => 'branch', :object => @leaf)) %>');

//animate new branch
$('#<%= @leaf.id %>')
  .hide()
  .slideDown('fast');

//bind events to the new branch
bindBranches();

//clear leaf form
$('#leaf_leaf_text').val('');
