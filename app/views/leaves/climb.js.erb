$('#level').html('Level: <%= "#{@leaf.level}/#{@leaf.max_length}" %>')

$('#story').append('<br/><%= @leaf.story %>');

$('#children').html('<%=
  escape_javascript(
    render(:partial => 'leaves/children', :object => @leaf, :as => :leaf)
  )
%>');

$('#leaf_parent_id').val('<%= @leaf.id %>');

$('#leaf_story').val('');
