<h1><%= @user.login %></h1>

<div id='follow'>
  <p><%= raw(follow(@user.id)) %></p>
</div>

<p>Points: <%= @user.points %></p>

<p>Followers: <%= @user.followers.count %></p>

<%= render :partial => 'branches/list', :object => @user.branches, :as => :branches %>
