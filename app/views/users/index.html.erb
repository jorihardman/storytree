<h1>Top Authors</h1>

<table>
  <tr>
    <th>Pen Name</th>
    <th>Points</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.login, author_path(user.id) %></td>
      <td><%= user.points %></td>
    </tr>
  <% end %>
</table>
